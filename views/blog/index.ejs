<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog | Arzani</title>
    <meta name="description" content="Explore the latest insights on business valuation, market trends, and growth strategies for UK small businesses.">
    
    <link rel="icon" href="/figma design exports/images.webp/arzani-icon-nobackground.png" type="image/png">
    <link rel="stylesheet" href="/css/modern.css">
    <link rel="stylesheet" href="/css/blog.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#316FD4',
                        dark: '#141414',
                        'dark-light': '#1E1E1E'
                    },
                    fontFamily: {
                        'sans': ['Inter', 'sans-serif']
                    }
                }
            }
        }
    </script>
    
    <!-- Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-W50GKB6F3M"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-W50GKB6F3M');
    </script>
</head>
<body class="bg-white text-gray-900 font-sans">
    <div class="page-wrapper">
       
        <%- include('../partials/navbar') %>

        <!-- Blog Hero Section -->
        <section class="relative pt-28 md:pt-36 pb-12 md:pb-16 bg-primary bg-opacity-5">
            <div class="absolute inset-0 z-0 opacity-5">
                <div class="grid-pattern"></div>
            </div>
            
            <div class="container mx-auto px-4 md:px-6 relative z-10">
                <div class="text-center max-w-3xl mx-auto mb-12">
                    <h1 class="text-4xl md:text-5xl font-bold mb-6 leading-tight text-gray-900">
                        The Arzani <span class="text-primary">Blog</span>
                    </h1>
                    <p class="text-xl text-gray-700 leading-relaxed">
                        Insights, trends, and expert advice on buying, selling, and scaling UK small businesses.
                    </p>
                </div>
                
                <!-- Featured Post -->
                <% if (featuredPost) { %>
                <div class="relative rounded-2xl overflow-hidden bg-white border border-gray-200 mb-16 animate-fade-in-up shadow-lg">
                    <div class="absolute inset-0 overflow-hidden opacity-10">
                        <img src="<%= featuredPost.hero_image || featuredPost.heroImage || 'https://arzani-images1.s3.eu-west-2.amazonaws.com/blogs/default-blog-hero.jpg' %>" 
                             alt="<%= featuredPost.title %>" 
                             class="w-full h-full object-cover"
                             onerror="this.onerror=null; this.src='https://arzani-images1.s3.eu-west-2.amazonaws.com/blogs/default-blog-hero.jpg';">
                        <div class="absolute inset-0 bg-gradient-to-t from-white via-white/80 to-transparent"></div>
                    </div>
                    
                    <div class="relative p-8 md:p-12 lg:p-16">
                        <div class="inline-block px-3 py-1 rounded-full bg-primary bg-opacity-10 text-primary text-sm font-medium mb-4">
                            Featured Article
                        </div>
                        
                        <h2 class="text-2xl md:text-3xl lg:text-4xl font-bold mb-4 max-w-3xl text-gray-900">
                            <%= featuredPost.title %>
                        </h2>
                        
                        <p class="text-lg text-gray-700 mb-6 max-w-2xl">
                            <%= featuredPost.excerpt %>
                        </p>
                        
                        <div class="flex items-center mb-8">
                            <img src="<%= featuredPost.author.avatar || '/figma design exports/images/default-avatar.png' %>" alt="<%= featuredPost.author.name %>" class="w-10 h-10 rounded-full mr-3 border border-gray-200">
                            <div>
                                <p class="font-medium text-gray-900"><%= featuredPost.author.name %></p>
                                <div class="text-sm text-gray-500 flex items-center">
                                    <span><%= new Date(featuredPost.publishDate).toLocaleDateString('en-GB', { day: 'numeric', month: 'long', year: 'numeric' }) %></span>
                                    <span class="mx-2">•</span>
                                    <span><%= featuredPost.readingTime %> min read</span>
                                </div>
                            </div>
                        </div>
                        
                        <a href="/blog/<%= featuredPost.slug %>" class="btn-primary">
                            Read Article
                        </a>
                    </div>
                </div>
                <% } %>
                
                <!-- Search and Filters -->
                <div class="flex flex-col md:flex-row gap-6 items-start mb-12">
                    <!-- Search Bar -->
                    <div class="w-full md:w-2/3">
                        <form action="/blog/search" method="GET" class="relative">
                            <input type="text" name="q" placeholder="Search articles..." class="w-full px-5 py-3 bg-white border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent">
                            <button type="submit" class="absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-400 hover:text-gray-600">
                                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                                </svg>
                            </button>
                        </form>
                    </div>
                    
                    <!-- Filters -->
                    <div class="w-full md:w-1/3 flex gap-3">
                        <div class="w-1/2">
                            <select id="categoryFilter" class="w-full px-4 py-3 bg-white border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent text-gray-700">
                                <option value="">All Categories</option>
                                <% categories.forEach(category => { %>
                                    <option value="<%= category.slug %>" <%= currentCategory === category.slug ? 'selected' : '' %>><%= category.name %></option>
                                <% }); %>
                            </select>
                        </div>
                        <div class="w-1/2">
                            <select id="sortFilter" class="w-full px-4 py-3 bg-white border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent text-gray-700">
                                <option value="recent">Most Recent</option>
                                <option value="popular">Most Popular</option>
                                <option value="trending">Trending</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Blog Posts Grid - Reduced image height by ~33% -->
        <section class="py-12 md:py-16 bg-white">
            <div class="container mx-auto px-4 md:px-6">
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                    <% blogPosts.forEach(post => { %>
                        <a href="/blog/<%= post.slug %>" class="block group animate-fade-in-up" style="animation-delay: '<%= blogPosts.indexOf(post) * 0.1 %>s'">
                            <article class="bg-white border border-gray-200 rounded-xl overflow-hidden transition-all group-hover:border-primary/30 h-full flex flex-col shadow-sm hover:shadow">
                                <div class="h-32 overflow-hidden">
                                    <img src="<%= post.hero_image || post.heroImage || 'https://arzani-images1.s3.eu-west-2.amazonaws.com/blogs/default-blog-hero.jpg' %>" 
                                         alt="<%= post.title %>" 
                                         class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
                                         onerror="this.onerror=null; this.src='https://arzani-images1.s3.eu-west-2.amazonaws.com/blogs/default-blog-hero.jpg';">
                                </div>
                                <div class="p-6 flex flex-col flex-grow">
                                    <div class="flex flex-wrap gap-2 mb-3">
                                        <% post.categories.slice(0, 2).forEach(category => { %>
                                            <span class="text-xs px-2 py-1 rounded-full bg-primary/10 text-primary">
                                                <%= category.name %>
                                            </span>
                                        <% }); %>
                                    </div>
                                    <h3 class="text-xl font-bold mb-3 line-clamp-2 text-gray-900"><%= post.title %></h3>
                                    <p class="text-gray-600 mb-6 line-clamp-3 flex-grow"><%= post.excerpt %></p>
                                    <div class="flex items-center justify-between mt-auto pt-4 border-t border-gray-100">
                                        <div class="flex items-center">
                                            <img src="<%= post.author.avatar || '/figma design exports/images/default-avatar.png' %>" alt="<%= post.author.name %>" class="w-8 h-8 rounded-full mr-2 border border-gray-200">
                                            <span class="text-sm text-gray-700"><%= post.author.name %></span>
                                        </div>
                                        <div class="text-sm text-gray-500">
                                            <%= new Date(post.publishDate).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' }) %>
                                        </div>
                                    </div>
                                </div>
                            </article>
                        </a>
                    <% }); %>
                </div>
                
                <!-- Pagination -->
                <div class="mt-16 flex justify-center">
                    <div class="inline-flex">
                        <% if (pagination.currentPage > 1) { %>
                            <a href="/blog?page=<%= pagination.currentPage - 1 %><%= currentCategory ? `&category=${currentCategory}` : '' %>" class="px-4 py-2 mx-1 text-sm rounded-lg border border-gray-300 hover:bg-gray-100 transition-colors text-gray-700">
                                Previous
                            </a>
                        <% } else { %>
                            <span class="px-4 py-2 mx-1 text-sm rounded-lg border border-gray-200 opacity-50 cursor-not-allowed text-gray-400">
                                Previous
                            </span>
                        <% } %>
                        
                        <% for (let i = 1; i <= pagination.totalPages; i++) { %>
                            <% if (i === pagination.currentPage) { %>
                                <span class="px-4 py-2 mx-1 text-sm rounded-lg bg-primary text-white font-medium">
                                    <%= i %>
                                </span>
                            <% } else { %>
                                <a href="/blog?page=<%= i %><%= currentCategory ? `&category=${currentCategory}` : '' %>" class="px-4 py-2 mx-1 text-sm rounded-lg border border-gray-300 hover:bg-gray-100 transition-colors text-gray-700">
                                    <%= i %>
                                </a>
                            <% } %>
                        <% } %>
                        
                        <% if (pagination.currentPage < pagination.totalPages) { %>
                            <a href="/blog?page=<%= pagination.currentPage + 1 %><%= currentCategory ? `&category=${currentCategory}` : '' %>" class="px-4 py-2 mx-1 text-sm rounded-lg border border-gray-300 hover:bg-gray-100 transition-colors text-gray-700">
                                Next
                            </a>
                        <% } else { %>
                            <span class="px-4 py-2 mx-1 text-sm rounded-lg border border-gray-200 opacity-50 cursor-not-allowed text-gray-400">
                                Next
                            </span>
                        <% } %>
                    </div>
                </div>
            </div>
        </section>
          <!-- Newsletter Section -->
        <% const newsletterSource = 'blog-index'; %>
        <%- include('../partials/newsletter') %>
        
        <!-- Categories Section -->
        <section class="py-16 bg-white">
            <div class="container mx-auto px-4 md:px-6">
                <h2 class="text-3xl font-bold mb-10 text-center text-gray-900">Browse by Category</h2>
                <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
                    <% categories.forEach(category => { %>
                        <a href="/blog/category/<%= category.slug %>" class="block group">
                            <div class="bg-white border border-gray-200 rounded-xl p-6 text-center transition-all group-hover:border-primary/30 group-hover:bg-gray-50 shadow-sm hover:shadow">
                                <div class="mb-3 text-primary text-2xl">
                                    <!-- Icon for the category - this could be customized per category -->
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <% if (category.slug === 'business-valuation') { %>
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
                                        <% } else if (category.slug === 'market-trends') { %>
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M7 12l3-3 3 3 4-4M8 21l4-4 4 4M3 4h18M4 4h16v12a1 1 0 01-1 1H5a1 1 0 01-1-1V4z" />
                                        <% } else if (category.slug === 'growth-strategies') { %>
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
                                        <% } else if (category.slug === 'selling-advice') { %>
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                                        <% } else if (category.slug === 'buying-advice') { %>
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
                                        <% } else { %>
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z" />
                                        <% } %>
                                    </svg>
                                </div>
                                <h3 class="text-lg font-semibold mb-2 text-gray-900">
                                    <%= category.name %>
                                </h3>
                                <p class="text-sm text-gray-500">
                                    <%= category.postCount %> articles
                                </p>
                            </div>
                        </a>
                    <% }); %>
                </div>
            </div>
        </section>

        <%- include('../partials/footer') %>
    </div>

    <script src="/js/navigation.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Add sticky header on scroll
            const header = document.querySelector('header');
            window.addEventListener('scroll', () => {
                if (window.scrollY > 50) {
                    header.classList.add('scrolled', 'bg-dark/80', 'backdrop-blur-md', 'border-b', 'border-gray-800');
                } else {
                    header.classList.remove('scrolled', 'bg-dark/80', 'backdrop-blur-md', 'border-b', 'border-gray-800');
                }
            });
            
            // Filter functionality
            const categoryFilter = document.getElementById('categoryFilter');
            const sortFilter = document.getElementById('sortFilter');
            
            if (categoryFilter) {
                categoryFilter.addEventListener('change', function() {
                    if (this.value) {
                        window.location.href = '/blog/category/' + this.value;
                    } else {
                        window.location.href = '/blog';
                    }
                });
            }
            
            if (sortFilter) {
                sortFilter.addEventListener('change', function() {
                    const searchParams = new URLSearchParams(window.location.search);
                    searchParams.set('sort', this.value);
                    window.location.href = window.location.pathname + '?' + searchParams.toString();
                });
                
                // Set current sort in dropdown
                const currentSort = new URLSearchParams(window.location.search).get('sort');
                if (currentSort) {
                    sortFilter.value = currentSort;
                }
            }
            
            // Mobile menu toggle
            const mobileMenuBtn = document.getElementById('mobileMenuBtn');
            const mobileMenu = document.getElementById('mobileMenu');
            
            if (mobileMenuBtn && mobileMenu) {
                mobileMenuBtn.addEventListener('click', function() {
                    const expanded = this.getAttribute('aria-expanded') === 'true';
                    this.setAttribute('aria-expanded', !expanded);
                    mobileMenu.classList.toggle('hidden');
                });
            }
        });
    </script>
</body>
</html>