<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results: <%= searchQuery %> | Arzani</title>
    <meta name="description" content="Search results for '<%= searchQuery %>' - Find relevant business insights and expert advice on Arzani Blog.">
    
    <link rel="icon" href="/figma design exports/images.webp/arzani-icon-nobackground.png" type="image/png">
    <link rel="stylesheet" href="/css/modern.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#316FD4',
                        dark: '#141414',
                        'dark-light': '#1E1E1E'
                    },
                    fontFamily: {
                        'sans': ['Inter', 'sans-serif']
                    }
                }
            }
        }
    </script>
    
    <!-- Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-W50GKB6F3M"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-W50GKB6F3M');
    </script>
</head>
<body class="bg-dark text-white font-sans">
    <div class="page-wrapper">
       
        <%- include('../partials/navbar') %>

        <!-- Search Results Header -->
        <section class="relative pt-28 md:pt-32 pb-10 md:pb-12 bg-dark-light">
            <div class="absolute inset-0 z-0 opacity-10">
                <div class="grid-pattern"></div>
            </div>
            
            <div class="container mx-auto px-4 md:px-6 relative z-10">
                <!-- Breadcrumbs -->
                <div class="mb-6 flex items-center text-sm text-gray-400">
                    <a href="/" class="hover:text-white transition-colors">Home</a>
                    <span class="mx-2">/</span>
                    <a href="/blog" class="hover:text-white transition-colors">Blog</a>
                    <span class="mx-2">/</span>
                    <span class="text-white">Search</span>
                </div>
                
                <h1 class="text-2xl sm:text-3xl md:text-4xl font-bold mb-6 leading-tight">
                    Search Results for: <span class="text-primary">"<%= searchQuery %>"</span>
                </h1>
                
                <p class="text-gray-300 mb-8">
                    Found <%= pagination.totalPosts %> results for your search
                </p>
                
                <!-- Search Bar -->
                <div class="max-w-2xl mb-8">
                    <form action="/blog/search" method="GET" class="relative">
                        <input type="text" name="q" value="<%= searchQuery %>" placeholder="Search articles..." class="w-full px-5 py-3 bg-dark-light border border-gray-800 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent">
                        <button type="submit" class="absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-400 hover:text-white">
                            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                            </svg>
                        </button>
                    </form>
                </div>
                
                <!-- Category Pills -->
                <div class="flex flex-wrap gap-2 mb-4">
                    <a href="/blog/search?q=<%= searchQuery %>" class="text-xs px-3 py-1 rounded-full <%= !currentCategory ? 'bg-primary text-white' : 'bg-dark-light border border-gray-700 text-gray-300 hover:bg-gray-800' %> transition-all">
                        All Results
                    </a>
                    <% categories.forEach(category => { %>
                        <a href="/blog/search?q=<%= searchQuery %>&category=<%= category.slug %>" class="text-xs px-3 py-1 rounded-full <%= currentCategory === category.slug ? 'bg-primary text-white' : 'bg-dark-light border border-gray-700 text-gray-300 hover:bg-gray-800' %> transition-all">
                            <%= category.name %>
                        </a>
                    <% }); %>
                </div>
            </div>
        </section>
        
        <!-- Search Results Content -->
        <section class="py-12 md:py-16 bg-dark">
            <div class="container mx-auto px-4 md:px-6">
                <% if (blogPosts.length === 0) { %>
                    <div class="text-center py-16">
                        <div class="mb-6">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto text-gray-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                        </div>
                        <h2 class="text-2xl font-bold mb-4">No results found</h2>
                        <p class="text-gray-400 mb-8 max-w-md mx-auto">
                            We couldn't find any articles matching your search. Please try different keywords or browse our categories.
                        </p>
                        <a href="/blog" class="btn-primary">Back to Blog</a>
                    </div>
                <% } else { %>
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                        <% blogPosts.forEach(post => { %>
                            <a href="/blog/<%= post.slug %>" class="block group animate-fade-in-up" style="animation-delay: '<%= blogPosts.indexOf(post) * 0.1 %>s'">
                                <article class="bg-dark-light/40 backdrop-blur-sm border border-gray-800 rounded-xl overflow-hidden transition-all group-hover:border-primary/30 h-full flex flex-col">
                                    <div class="h-48 overflow-hidden">
                                        <img src="<%= post.heroImage %>" alt="<%= post.title %>" class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105">
                                    </div>
                                    <div class="p-6 flex flex-col flex-grow">
                                        <div class="flex flex-wrap gap-2 mb-3">
                                            <% post.categories.slice(0, 2).forEach(category => { %>
                                                <span class="text-xs px-2 py-1 rounded-full bg-primary/10 text-primary">
                                                    <%= category.name %>
                                                </span>
                                            <% }); %>
                                        </div>
                                        <h3 class="text-xl font-bold mb-3 line-clamp-2"><%= post.title %></h3>
                                        <p class="text-gray-400 mb-6 line-clamp-3 flex-grow"><%= post.excerpt %></p>
                                        <div class="flex items-center justify-between mt-auto pt-4 border-t border-gray-800">
                                            <div class="flex items-center">
                                                <img src="<%= post.author.avatar || '/figma design exports/images/default-avatar.png' %>" alt="<%= post.author.name %>" class="w-8 h-8 rounded-full mr-2 border border-gray-700">
                                                <span class="text-sm"><%= post.author.name %></span>
                                            </div>
                                            <div class="text-sm text-gray-500">
                                                <%= new Date(post.publishDate).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' }) %>
                                            </div>
                                        </div>
                                    </div>
                                </article>
                            </a>
                        <% }); %>
                    </div>
                    
                    <!-- Pagination -->
                    <% if (pagination.totalPages > 1) { %>
                    <div class="mt-16 flex justify-center">
                        <div class="inline-flex">
                            <% if (pagination.currentPage > 1) { %>
                                <a href="/blog/search?q=<%= searchQuery %>&page=<%= pagination.currentPage - 1 %><%= currentCategory ? `&category=${currentCategory}` : '' %>" class="px-4 py-2 mx-1 text-sm rounded-lg border border-gray-800 hover:bg-dark-light/40 transition-colors">
                                    Previous
                                </a>
                            <% } else { %>
                                <span class="px-4 py-2 mx-1 text-sm rounded-lg border border-gray-800 opacity-50 cursor-not-allowed">
                                    Previous
                                </span>
                            <% } %>
                            
                            <% for (let i = 1; i <= pagination.totalPages; i++) { %>
                                <% if (i === pagination.currentPage) { %>
                                    <span class="px-4 py-2 mx-1 text-sm rounded-lg bg-primary text-white font-medium">
                                        <%= i %>
                                    </span>
                                <% } else { %>
                                    <a href="/blog/search?q=<%= searchQuery %>&page=<%= i %><%= currentCategory ? `&category=${currentCategory}` : '' %>" class="px-4 py-2 mx-1 text-sm rounded-lg border border-gray-800 hover:bg-dark-light/40 transition-colors">
                                        <%= i %>
                                    </a>
                                <% } %>
                            <% } %>
                            
                            <% if (pagination.currentPage < pagination.totalPages) { %>
                                <a href="/blog/search?q=<%= searchQuery %>&page=<%= pagination.currentPage + 1 %><%= currentCategory ? `&category=${currentCategory}` : '' %>" class="px-4 py-2 mx-1 text-sm rounded-lg border border-gray-800 hover:bg-dark-light/40 transition-colors">
                                    Next
                                </a>
                            <% } else { %>
                                <span class="px-4 py-2 mx-1 text-sm rounded-lg border border-gray-800 opacity-50 cursor-not-allowed">
                                    Next
                                </span>
                            <% } %>
                        </div>
                    </div>
                    <% } %>
                <% } %>
                
                <!-- Related Topics / Tags -->
                <div class="mt-16">
                    <h3 class="text-xl font-bold mb-6">Related Topics</h3>
                    <div class="flex flex-wrap gap-3">
                        <% categories.slice(0, 8).forEach(category => { %>
                            <a href="/blog/category/<%= category.slug %>" class="px-4 py-2 rounded-lg bg-dark-light/60 border border-gray-800 hover:bg-dark-light transition-colors">
                                <%= category.name %>
                            </a>
                        <% }); %>
                    </div>
                </div>
            </div>
        </section>

        <%- include('../partials/footer') %>
    </div>

    <script src="/js/navigation.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Add sticky header on scroll
            const header = document.querySelector('header');
            window.addEventListener('scroll', () => {
                if (window.scrollY > 50) {
                    header.classList.add('scrolled', 'bg-dark/80', 'backdrop-blur-md', 'border-b', 'border-gray-800');
                } else {
                    header.classList.remove('scrolled', 'bg-dark/80', 'backdrop-blur-md', 'border-b', 'border-gray-800');
                }
            });
            
            // Mobile menu toggle
            const mobileMenuBtn = document.getElementById('mobileMenuBtn');
            const mobileMenu = document.getElementById('mobileMenu');
            
            if (mobileMenuBtn && mobileMenu) {
                mobileMenuBtn.addEventListener('click', function() {
                    const expanded = this.getAttribute('aria-expanded') === 'true';
                    this.setAttribute('aria-expanded', !expanded);
                    mobileMenu.classList.toggle('hidden');
                });
            }
        });
    </script>
</body>
</html>