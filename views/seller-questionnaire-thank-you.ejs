<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You | Arzani Marketplace</title>
    <link rel="icon" href="/figma design exports/images.webp/arzani-icon-nobackground.png" type="image/png">

    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700;900&display=swap" rel="stylesheet">
    <%- include('../views/partials/tailwind-setup') %>
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: white;
        }
        .header {
            height: 87px;
            border-bottom: 0.8px solid #bfbcc6;
        }
        .btn-main {
            background-color: #25224a;
            color: white;
            border-radius: 0 1rem 1rem 1rem;
        }
        .btn-outline {
            border: 2px solid #25224a;
            color: #25224a;
            border-radius: 0 1rem 1rem 1rem;
        }
        .btn-outline:hover {
            background-color: #f0f4ff;
        }
        .exit-link {
            color: #25224a;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
    </style>
    <!-- Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-W50GKB6F3M"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-W50GKB6F3M');
    </script>
</head>
<body>
    <div class="w-full min-h-screen flex flex-col bg-white">
        <!-- Header -->
        <header class="header w-full bg-white flex items-center flex-shrink-0">
            <div class="container mx-auto px-4 md:px-8 flex items-center justify-between">
                <!-- Logo -->
                <div class="h-12 md:h-16">
                    <img src="/figma design exports/logos/Arzani black-white background 2.png" alt="Arzani" class="h-full">
                </div>
                
                <!-- Right section -->
                <div class="flex items-center gap-4">
                    <!-- Exit link -->
                    <a href="/" class="exit-link">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
                            <polyline points="16 17 21 12 16 7"></polyline>
                            <line x1="21" y1="12" x2="9" y2="12"></line>
                        </svg>
                        <span class="hidden sm:inline">Home</span>
                    </a>
                </div>
            </div>
        </header>

        <!-- Main content -->
        <main class="flex-grow flex items-center justify-center p-4">
            <div class="max-w-2xl w-full bg-white p-8 rounded-xl shadow-lg">
                <div class="text-center mb-8">
                    <div class="inline-flex items-center justify-center w-16 h-16 bg-green-100 rounded-full mb-4">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                        </svg>
                    </div>
                    <h1 class="text-3xl font-bold text-[#25224a] mb-2">Thank You!</h1>
                    <p class="text-[#5f5770] text-lg">For taking time to complete the questionnaire.</p>
                </div>
                
                <div class="bg-blue-50 p-6 rounded-lg mb-8">
                    <h3 class="text-xl font-bold text-[#25224a] mb-3">What's Next?</h3>
                    <p class="text-[#5f5770] mb-4">
                        To access your full valuation report and list your business on our marketplace, you can create an account anytime.
                    </p>
                    <p class="text-[#5f5770]">
                        When you sign up, all your information will be automatically connected to your account.
                    </p>
                </div>
                
                <div class="flex flex-col md:flex-row gap-4 justify-center">
                    <a href="/signup" class="btn-main py-3 px-6 text-center font-medium text-lg">
                        Create Account Now
                    </a>
                    <a href="/" class="btn-outline py-3 px-6 text-center font-medium text-lg">
                        Back to Homepage
                    </a>
                </div>
                
                <div class="text-center mt-8 text-sm text-[#8e8a9a]">
                    <p>
                        Already have an account? <a href="/login" class="text-[#3e50f7] font-medium">Log in</a> to access your information.
                    </p>
                </div>
            </div>
        </main>
        
        <!-- Footer -->
        <footer class="bg-white py-8 border-t border-[#bfbcc6]">
            <div class="container mx-auto px-4 text-center text-[#5f5770]">
                <p>&copy; <%= new Date().getFullYear() %> Arzani Marketplace. All rights reserved.</p>
            </div>
        </footer>
    </div>

    <!-- Add script to handle pending questionnaire data -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Check if we got here after a login redirect
            const justLoggedIn = localStorage.getItem('loginRedirectWithData') === 'true';
            const loginTime = localStorage.getItem('loginRedirectTime');
            
            // Check if we have pending questionnaire data in localStorage
            const pendingData = localStorage.getItem('pendingQuestionnaireData');
            const pendingSaveTime = localStorage.getItem('pendingQuestionnaireSaveTime');
            
            // Log status for debugging
            console.log('Thank you page loaded');
            console.log('Just logged in:', justLoggedIn);
            console.log('Has pending data:', !!pendingData);
            
            if (pendingData) {
                try {
                    // Check if the data was saved recently (within the last hour)
                    // or if we just logged in (which takes priority)
                    const saveTime = new Date(pendingSaveTime || Date.now());
                    const hourAgo = new Date(Date.now() - (60 * 60 * 1000));
                    
                    if (justLoggedIn || saveTime > hourAgo) {
                        console.log('Found pending questionnaire data to submit');
                        
                        // Mark as success now that the user has reached the thank-you page
                        localStorage.setItem('questionnaireDataSubmitted', 'true');
                        
                        // Clear login redirect flags
                        localStorage.removeItem('loginRedirectWithData');
                        localStorage.removeItem('loginRedirectTime');
                        
                        // Create a status indicator for the user
                        const mainContent = document.querySelector('main');
                        if (mainContent) {
                            const statusDiv = document.createElement('div');
                            statusDiv.className = 'mb-4 w-full bg-blue-50 p-4 rounded-lg text-center';
                            statusDiv.id = 'dataSubmissionStatus';
                            statusDiv.innerHTML = '<p class="text-blue-700">Saving your questionnaire data...</p>';
                            mainContent.querySelector('.max-w-2xl').prepend(statusDiv);
                        }
                        
                        // Try to submit data again in the background
                        // This will likely succeed now that the user is logged in
                        fetch('/api/business/save-questionnaire', {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json',
                            },
                            body: pendingData,
                            credentials: 'include' // Include cookies for auth
                        })
                        .then(response => {
                            if (response.ok) return response.json();
                            throw new Error('Failed to save data: ' + response.status);
                        })
                        .then(result => {
                            if (result.success) {
                                console.log('Successfully submitted pending questionnaire data with ID:', result.submissionId);
                                localStorage.setItem('questionnaireSubmissionId', result.submissionId);
                                
                                // Update the status indicator
                                const statusDiv = document.getElementById('dataSubmissionStatus');
                                if (statusDiv) {
                                    statusDiv.className = 'mb-4 w-full bg-green-50 p-4 rounded-lg text-center';
                                    statusDiv.innerHTML = '<p class="text-green-700">Your questionnaire data has been saved successfully!</p>';
                                }
                                
                                // Clear pending data
                                localStorage.removeItem('pendingQuestionnaireData');
                                localStorage.removeItem('pendingQuestionnaireSaveTime');
                            }
                        })
                        .catch(error => {
                            console.warn('Background submission of pending data failed:', error);
                            // Update the status indicator
                            const statusDiv = document.getElementById('dataSubmissionStatus');
                            if (statusDiv) {
                                statusDiv.className = 'mb-4 w-full bg-yellow-50 p-4 rounded-lg text-center';
                                statusDiv.innerHTML = `
                                    <p class="text-yellow-700">We couldn't save your data right now.</p>
                                    <p class="text-sm text-yellow-700">Don't worry, you can still create an account to access your valuation.</p>
                                `;
                            }
                            // Keep the pending data in case the user logs in later
                        });
                    } else {
                        // Data is too old, clear it
                        localStorage.removeItem('pendingQuestionnaireData');
                        localStorage.removeItem('pendingQuestionnaireSaveTime');
                    }
                } catch (error) {
                    console.error('Error processing pending questionnaire data:', error);
                }
            }
        });
    </script>
</body>
</html>
