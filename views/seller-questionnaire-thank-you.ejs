<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You | Arzani Marketplace</title>
    <link rel="icon" href="/figma design exports/images.webp/arzani-icon-nobackground.png" type="image/png">

    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700;900&display=swap" rel="stylesheet">
    <%- include('../views/partials/tailwind-setup') %>
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: white;
        }
        .header {
            height: 87px;
            border-bottom: 0.8px solid #bfbcc6;
        }
        .btn-main {
            background-color: #25224a;
            color: white;
            border-radius: 0 1rem 1rem 1rem;
        }
        .btn-outline {
            border: 2px solid #25224a;
            color: #25224a;
            border-radius: 0 1rem 1rem 1rem;
        }
        .btn-outline:hover {
            background-color: #f0f4ff;
        }
        .exit-link {
            color: #25224a;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
    </style>
    <!-- Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-W50GKB6F3M"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-W50GKB6F3M');
    </script>
</head>
<body>
    <div class="w-full min-h-screen flex flex-col bg-white">
        <!-- Header -->
        <header class="header w-full bg-white flex items-center flex-shrink-0">
            <div class="container mx-auto px-4 md:px-8 flex items-center justify-between">
                <!-- Logo -->
                <div class="h-12 md:h-16">
                    <img src="/figma design exports/logos/Arzani black-white background 2.png" alt="Arzani" class="h-full">
                </div>
                
                <!-- Right section -->
                <div class="flex items-center gap-4">
                    <!-- Exit link -->
                    <a href="/" class="exit-link">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
                            <polyline points="16 17 21 12 16 7"></polyline>
                            <line x1="21" y1="12" x2="9" y2="12"></line>
                        </svg>
                        <span class="hidden sm:inline">Home</span>
                    </a>
                </div>
            </div>
        </header>

        <!-- Main content -->
        <main class="flex-grow flex items-center justify-center p-4">
            <div class="max-w-2xl w-full bg-white p-8 rounded-xl shadow-lg">
                <div class="text-center mb-8">
                    <div class="inline-flex items-center justify-center w-16 h-16 bg-green-100 rounded-full mb-4">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                        </svg>
                    </div>
                    <h1 class="text-3xl font-bold text-[#25224a] mb-2">Thank You!</h1>
                    <p class="text-[#5f5770] text-lg">For taking time to complete the questionnaire.</p>
                </div>
                
                <div class="bg-blue-50 p-6 rounded-lg mb-8">
                    <h3 class="text-xl font-bold text-[#25224a] mb-3">What's Next?</h3>
                    <p class="text-[#5f5770] mb-4">
                        To access your full valuation report and list your business on our marketplace, you can create an account anytime.
                    </p>
                    <p class="text-[#5f5770]">
                        When you sign up, all your information will be automatically connected to your account.
                    </p>
                </div>
                
                <div class="flex flex-col md:flex-row gap-4 justify-center">
                    <a href="/signup" class="btn-main py-3 px-6 text-center font-medium text-lg">
                        Create Account Now
                    </a>
                    <a href="/" class="btn-outline py-3 px-6 text-center font-medium text-lg">
                        Back to Homepage
                    </a>
                </div>
                
                <div class="text-center mt-8 text-sm text-[#8e8a9a]">
                    <p>
                        Already have an account? <a href="/login" class="text-[#3e50f7] font-medium">Log in</a> to access your information.
                    </p>
                </div>
            </div>
        </main>
        
        <!-- Footer -->
        <footer class="bg-white py-8 border-t border-[#bfbcc6]">
            <div class="container mx-auto px-4 text-center text-[#5f5770]">
                <p>&copy; <%= new Date().getFullYear() %> Arzani Marketplace. All rights reserved.</p>
            </div>
        </footer>
    </div>

    <!-- Add script to handle pending questionnaire data -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            console.log('Thank-you page loaded');
            
            // Add status div for visual feedback
            const mainContent = document.querySelector('main .max-w-2xl');
            if (mainContent) {
                const statusDiv = document.createElement('div');
                statusDiv.id = 'statusMessage';
                statusDiv.className = 'mb-4 p-4 rounded-lg text-center hidden';
                mainContent.prepend(statusDiv);
            }
            
            // Function to show status message
            function showStatus(message, type = 'info') {
                const statusDiv = document.getElementById('statusMessage');
                if (!statusDiv) return;
                
                let bgColor = 'bg-blue-50';
                let textColor = 'text-blue-700';
                
                if (type === 'success') {
                    bgColor = 'bg-green-50';
                    textColor = 'text-green-700';
                } else if (type === 'warning') {
                    bgColor = 'bg-yellow-50';
                    textColor = 'text-yellow-700';
                } else if (type === 'error') {
                    bgColor = 'bg-red-50';
                    textColor = 'text-red-700';
                }
                
                statusDiv.className = `mb-4 p-4 rounded-lg text-center ${bgColor}`;
                statusDiv.innerHTML = `<p class="${textColor}">${message}</p>`;
                statusDiv.classList.remove('hidden');
            }
            
            // Check for questionnaire flags
            const completedQuestionnaire = localStorage.getItem('questionnaireCompleted') === 'true';
            const hasAnonymousId = Boolean(localStorage.getItem('questionnaireAnonymousId'));
            const hasValuationData = Boolean(localStorage.getItem('sellerValuationData'));
            const pendingData = localStorage.getItem('pendingQuestionnaireData');
            
            console.log('Questionnaire completed:', completedQuestionnaire);
            console.log('Has anonymous ID:', hasAnonymousId);
            console.log('Has valuation data:', hasValuationData);
            console.log('Has pending data:', Boolean(pendingData));
            
            if (!completedQuestionnaire && !hasValuationData) {
                showStatus('It looks like you haven\'t completed the questionnaire yet.', 'warning');
            } else if (hasValuationData) {
                showStatus('Your valuation has been calculated successfully!', 'success');
            }
            
            // Try to submit pending data if available
            if (pendingData) {
                try {
                    const pendingSaveTime = localStorage.getItem('pendingQuestionnaireSaveTime');
                    const saveTime = new Date(pendingSaveTime || Date.now());
                    const hourAgo = new Date(Date.now() - (60 * 60 * 1000));
                    
                    if (saveTime > hourAgo) {
                        showStatus('Saving your questionnaire data...', 'info');
                        
                        // Attempt to save the data
                        fetch('/api/business/save-questionnaire', {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json',
                            },
                            body: pendingData,
                            credentials: 'include' // Include cookies for auth
                        })
                        .then(response => {
                            if (!response.ok) {
                                if (response.status === 401 || response.status === 403) {
                                    return { success: false, message: 'Authentication required' };
                                }
                                throw new Error(`HTTP error: ${response.status}`);
                            }
                            return response.json();
                        })
                        .then(result => {
                            if (result.success) {
                                showStatus('You havee completed your valuation!', 'success');
                                localStorage.setItem('questionnaireSubmissionId', result.submissionId);
                                localStorage.removeItem('pendingQuestionnaireData');
                                localStorage.removeItem('pendingQuestionnaireSaveTime');
                            } else {
                                showStatus('Your data is saved locally and will be linked to your account when you sign up.', 'info');
                            }
                        })
                        .catch(error => {
                            console.warn('Background submission failed:', error);
                            showStatus('Your data is saved locally and will be linked to your account when you sign up.', 'info');
                        });
                    } else {
                        // Data is too old, clear it
                        localStorage.removeItem('pendingQuestionnaireData');
                        localStorage.removeItem('pendingQuestionnaireSaveTime');
                    }
                } catch (error) {
                    console.error('Error processing pending data:', error);
                }
            }
            
            // Pre-fill email on signup link
            const signupLink = document.querySelector('a[href="/signup"]');
            const email = localStorage.getItem('sellerEmail');
            if (signupLink && email) {
                signupLink.href = `/signup?email=${encodeURIComponent(email)}`;
            }
        });
    </script>
</body>
</html>
