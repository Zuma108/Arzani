name: Main Workflow
# This is a placeholder to avoid Git conflicts
# The actual deployment should use azure-workflow.yml

on:
  workflow_dispatch:  # Only run manually

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
    - name: Note about workflows
      run: |
        echo "This workflow is deprecated."
        echo "Please use azure-workflow.yml for deployment and ci.yml for testing."
