name: Azure Deployment

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: Arzani    # set to your application's name
  AZURE_WEBAPP_PACKAGE_PATH: '.'           # repository root
  NODE_VERSION: '18.x'                     # matches your engine requirements in package.json

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        # Improve caching mechanism
        cache: 'npm'
        cache-dependency-path: '**/package-lock.json'

    - name: npm install, build, and test
      run: |
        npm ci
        npm run build --if-present
        npm test -- --passWithNoTests
      env:
        # Set up environment variables for testing
        NODE_ENV: test
        DATABASE_URL: ${{ secrets.TEST_DATABASE_URL }}
        JWT_SECRET: ${{ secrets.JWT_SECRET }}

    - name: Upload artifact for deployment job
      uses: actions/upload-artifact@v4
      with:
        name: node-app
        path: |
          .
          !node_modules
          !tests
          !.git
          !.github
          !*.md
          !*.env

  deploy:
    permissions:
      contents: none
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Download artifact from build job
      uses: actions/download-artifact@v4
      with:
        name: node-app

    - name: Install production dependencies
      run: npm ci --only=production
      
    # Add brief delay before deployment to ensure any in-progress operations complete
    - name: Wait before deployment
      run: sleep 30
      
    - name: 'Deploy to Azure WebApp'
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        package: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}
        enable-oryx-build: true
        app-settings: |
          SCM_DO_BUILD_DURING_DEPLOYMENT=true
          WEBSITE_NODE_DEFAULT_VERSION=~18.19.0
          WEBSITE_WEBDEPLOY_USE_SCM=true
    
    # Add retry logic in case of initial deployment failure
    - name: 'Retry deployment if needed'
      if: failure() && steps.deploy-to-webapp.conclusion == 'failure'
      run: |
        echo "First deployment attempt failed, waiting 60 seconds before retry..."
        sleep 60
    
    - name: 'Retry Azure WebApp Deployment'
      if: failure() && steps.deploy-to-webapp.conclusion == 'failure'
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        package: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}
        enable-oryx-build: true
        app-settings: |
          SCM_DO_BUILD_DURING_DEPLOYMENT=true
          WEBSITE_NODE_DEFAULT_VERSION=~18.19.0
          WEBSITE_WEBDEPLOY_USE_SCM=true